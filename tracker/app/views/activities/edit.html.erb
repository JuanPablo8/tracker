<div class="edit_card">

<h1 class="title_2">Edit Activity</h1>

<%= form_with(model: @activity, local: true) do |form| %>

  <% if @activity.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@activity.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @activity.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>




  <p>
    <div class="form_label"><%= form.label :Title %><br></div>
    <div class="form_text"><%= form.text_field :title %></div>
  </p>

    <p>
    <div class="boton4"> <%= form.submit %></div>
    </p>

<% end %>

  <button class="action-back"><a><%= link_to 'Back', activities_path %></a></button>

</div>